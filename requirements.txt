# Sistema de prediccion del prode del Mundial 2026 (modulos *_wc).
# Stack liviano: numpy para el motor probabilistico (Dixon-Coles, optimizador, Monte
# Carlo) y requests para el fetch. Sin frameworks pesados.
numpy>=1.24
requests>=2.28
# Opcionales:
# scipy>=1.10           # si se agregan calibraciones (Platt/isotonica) o solvers extra
# beautifulsoup4>=4.12  # solo si se scrapea HTML en el camino en vivo de fetch_wc
# lxml>=4.9
